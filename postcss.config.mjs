const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
export default config;
// This configuration file sets up PostCSS to use the Tailwind CSS plugin.
// It exports a configuration object that includes the Tailwind CSS plugin.
// The '@tailwindcss/postcss' plugin is used to process Tailwind CSS styles.
